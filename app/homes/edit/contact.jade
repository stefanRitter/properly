.center
  h2.italic.margin Add Contact Information

  form.form(name="homeContactForm")
    .form-group
      label How may Properly-verified tenants contact you?*
      br
      input(type="checkbox" name="longRent" value="1" ng-model="home.contactEmail" checked)
      | Properly Email
      br
      input(type="checkbox" name="shortRent" value="1" ng-model="home.contactPhone")
      | Telephone

    .form-group
      button.button-red.medium.home-edit-next(ng-click="verify()") Next
      br
      span.required * required

    .form-group
      .error {{notifier.error}}
      .notice {{notifier.notice}}
